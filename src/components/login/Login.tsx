import React from 'react';

export const Login = () => {
    return (
        <div>
            <input  placeholder={'email'} type="text"/><input placeholder={'pass'} type="password"/>
            <button>send</button>
        </div>
    );
};
